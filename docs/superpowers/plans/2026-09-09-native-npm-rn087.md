# Native npm / React Native 0.87.1

**Objetivo:** cumplir la incidencia facilitada por el usuario en la rama `fix/native-npm-rn-087`, conservando React Native 0.87.1, React 19.2.3 y TypeScript 6.0.3 en el consumidor.

**Diseño:** conservar la API pública, generar tarballs con pnpm y verificar su contenido antes de publicar el mismo artefacto. El inicializador selecciona una receta compatible por versión de RN y conserva configuración y providers existentes. La app de validación vive fuera del monorepo y usa npm y artefactos, nunca enlaces al código fuente.

- [x] Reproducir npm 0.1.1, auditar manifiestos, imports y documentación de compatibilidad.
- [x] Añadir regresiones de empaquetado y del inicializador para RN 0.87.1, configuración previa, repetición y dry-run.
- [x] Corregir versiones/dependencias y verificar tarballs (exports, JS, tipos, recursos, protocolos internos); proteger publicación desde directorios.
- [x] Actualizar la receta nativa, Babel/Metro, estilos y composición de providers.
- [x] Instalar los artefactos con npm fuera del workspace, comprobar tipos/lint y bundles Android/iOS; intentar build y ejecución Android según herramientas disponibles.
- [x] Actualizar README, documentación web, guía de publicación, changelog y evidencia con límites por plataforma.
- [x] Ejecutar typecheck, lint, tests relevantes y revisión final. No publicar ni hacer push sin confirmación.

Archivos principales: `scripts/release.mjs`, `scripts/release.test.mjs`, manifiestos de paquetes, `packages/init/src/{project,templates,transforms}.mjs`, `packages/init/test/init.test.mjs`, `packages/native/README.md`, `packages/init/README.md`, `docs/publish-and-version.md`, `docs/native-installation.md` y `CHANGELOG.md`.

Verificación: `node --test scripts/release.test.mjs`, `pnpm --filter @kivora/init test`, `pnpm release:prepare`, `pnpm typecheck`, `pnpm lint`, `pnpm --filter './packages/**' test`; en la app externa, `npm install`, `npm ls`, `npm run typecheck`, `npm run lint`, bundles con `react-native bundle` y Gradle `assembleDebug`. iOS requiere macOS/Xcode para compilar y ejecutar.
